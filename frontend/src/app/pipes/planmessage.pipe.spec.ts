import { PlanmessagePipe } from '../planmessage.pipe';

describe('PlanmessagePipe', () => {
  it('create an instance', () => {
    const pipe = new PlanmessagePipe();
    expect(pipe).toBeTruthy();
  });
});
